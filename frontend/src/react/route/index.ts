/**
 * Exports the BrowserRouter configuration.
 *
 * This module exports the BrowserRouter configuration, which sets up the routing for the application using React Router.
 * It defines the routes and their corresponding components, including public and private routes, as well as error handling.
 *
 * @module BrowserRouterExport
 */

export { default as BrowserRouter } from "./BrowserRouter";
/**
 * BrowserRouter configuration.
 *
 * This export provides the BrowserRouter configuration for the application.
 *
 * @exports BrowserRouter
 */
